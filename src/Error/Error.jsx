import React, { memo } from 'react';
import { Placeholder } from 'components';

const Error = memo(() => (
  <Placeholder
    icon="error"
    className="error"
  >
      Error
  </Placeholder>
));

export default Error;
